package com.springboot.repository.impl;

import com.springboot.repository.SysUserRepository;
import org.springframework.data.jpa.repository.Query;

public class SysUserRepositoryImpl {

   // @Query(value="SELECT * FROM SysUser AS a WHERE a.UName like '' AND a.isDeleted='' AND a.sysRole.Role ")

}
